stages:
  - test

run-csharp-ios-tests:
  stage: test
  rules:
    - when: always
  tags:
    - iPhone
    - mac
    - unity
    - xcode
  script:
    - chmod +x ./ios_tests.sh
  artifacts:
    when: always
    expire_in: 10 days